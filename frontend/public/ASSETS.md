# Branding Assets

Place your custom images here before building:

- `logo.png` — Site logo (navbar, landing, login, register)
- `bg.jpg` — Dark background (landing, login, register, dashboard)

Then rebuild Docker:

```bash
docker compose down
docker compose up --build -d
```
